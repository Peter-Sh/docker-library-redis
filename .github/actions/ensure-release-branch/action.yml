inputs:
  release_tag:
    description: 'Release tag to build'
    required: true

runs:
  using: "composite"
  steps:
    - name: Ensure Release Branch
      shell: bash
      run: |
        ${{ github.action_path }}/ensure-release-branch.sh ${{ inputs.release_tag }}
